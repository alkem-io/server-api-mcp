apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: alkemio-mcp-timeout
spec:
  headers:
    accessControlAllowMethods:
      - GET
      - POST
      - OPTIONS
    accessControlAllowOriginList:
      - "*"
    accessControlMaxAge: 3600
    addVaryHeader: true