{
  "name": "server-api-mcp",
  "version": "0.0.1",
  "description": "server-api-mcp MCP server",
  "type": "module",
  "bin": {
    "server-api-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && mcp-build",
    "watch": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@alkemio/client-lib": "^0.35.0",
    "dotenv": "^16.5.0",
    "mcp-framework": "^0.2.13"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.19.0"
  }
}
